\ProvidesPackage{UTairJSCA5}

\documentclass[a5paper,9pt]{article}
\usepackage{cmap}
\usepackage[utf8]{inputenc} % Кодировка utf8
\usepackage{amsmath,amssymb}
\usepackage{indentfirst}
\usepackage[english,russian]{babel}
\usepackage[T2A]{fontenc}

\usepackage[inner=1cm, outer=1cm,top=18mm,bottom=18mm]{geometry}%top=headheight+margin4mm+8
\usepackage{graphicx}%Вставка картинок правильная
\usepackage{float}%"Плавающие" картинки
\usepackage{color}
\usepackage{lastpage}
\usepackage{multirow}
\usepackage[colorlinks=true, linkcolor=blue]{hyperref} %==подключение и определение гиперссылок в доках

%===параметры абзаца =================================
\setlength{\parindent}{5mm} %размер отступа первой строки абзаца
\setlength{\parskip}{3pt} %=отступы после абзацев



%=оформление списков:==============
%\usepackage{enumerate}
\usepackage{enumitem}
\setlist{noitemsep}
\setlist[itemize]{leftmargin=5mm}
\setlist[enumerate]{leftmargin=5mm}
%\setlist{leftmargin=10mm}
%\setlist{labelindent=10mm} % < Usually a good idea
%\setlist[itemize,1]{label=$\triangleleft$}
%\setlist[enumerate,1]{label=\arabic*., ref=\arabic*}
%==конец оформления списков=========

%===========Колонтитулы====================
\usepackage{fancybox,fancyhdr}
\renewcommand{\headrulewidth}{1pt}
\renewcommand{\footrulewidth}{1pt}
%==Команды для вставки в колонтитулы переменных данных
\newcommand{\UTdocHead}[1]{\fancyhead[L]{\includegraphics[width=0.1\textwidth]{../utlogo.png}}
    \fancyhead[C]{}
    \fancyhead[R]{\textcolor{blue}{\footnotesize{ РПП АО ЮТэйр\\#1}}}}%==Название документа

\newcommand{\UTdocPageFooter}[2]{\fancyfoot[L]{\footnotesize{Дата редакции:#2}}
    \fancyfoot[C]{}
    \fancyfoot[R]{\footnotesize{#1.\arabic{page} из \pageref{LastPage}~}}}%==страницы
%=============конец колонтитулов===============


%== нумерует новый параграф, увеличивает счетик на единицу. 
%== Subsect должен обнуляться принудительно обнулятьсяпосле объявления раздела
\newcommand{\newPar}{\paragraph{\arabic{section}.\arabic{subsect} } \stepcounter{subsect}}


%== для внесения изменений в текст. Оформляется синим, сбоку вертикальная линия
%== вносятся данные в нижний колонтитул и он определяется только для страницы с изм параграфом
\newcommand{\chgdPar}[3]{\marginpar{\rule[-8ex]{1pt}{7ex}}%
     \textcolor[rgb]{0,0,1}{#3}
     \fancypagestyle{fplain}{
     \fancyfoot[L]{\footnotesize{Дата редакции:#1}}
     \fancyfoot[C]{\footnotesize{Изменение №:#2}}}
     \thispagestyle{fplain} }
